import React from 'react';

/**
 * Used both for creating a new client and editing an existing one.
 * When accessed via /clients/maintenance/:id the route param `id` is available.
 */
function ClientMaintenancePage() {
  return <div>ClientMaintenancePage — placeholder</div>;
}

export default ClientMaintenancePage;
